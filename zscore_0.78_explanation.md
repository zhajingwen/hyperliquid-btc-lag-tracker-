# Z-score = 0.78 的含义解释

## 基本定义

Z-score（标准分数）表示**当前价差偏离历史均值的标准差倍数**。

### 计算公式

```
Z-score = (当前价差 - 历史均值) / 历史标准差
```

其中：
- **当前价差** = log(ETHFI价格) - β × log(BTC价格)
- **历史均值** = 最近20个数据点的价差平均值
- **历史标准差** = 最近20个数据点的价差标准差
- **β（Beta）** = 基于对数价格的Beta系数

---

## Z-score = 0.78 的具体含义

### 1. 数值解释

**Z-score = 0.78** 表示：
- 当前价差比历史均值**高0.78个标准差**
- 这是一个**正值**，说明当前价差**高于历史平均水平**

### 2. 统计学意义

在正态分布假设下：
- **Z = 0.78** 意味着当前价差处于历史分布的**第78.2百分位数**
- 大约有**78.2%的历史数据**的价差低于当前值
- 大约有**21.8%的历史数据**的价差高于当前值

### 3. 偏离程度评估

根据系统设定的阈值标准：

| Z-score范围 | 偏离程度 | 套利信号强度 | ETHFI的状态 |
|------------|---------|------------|------------|
| \|Z\| > 3 | 极强偏离 | 强套利信号 | - |
| 2 < \|Z\| ≤ 3 | 显著偏离 | 中等套利信号 | - |
| **1 < \|Z\| ≤ 2** | **中等偏离** | **弱套利信号** | **ETHFI在这里** |
| **\|Z\| ≤ 1** | **正常波动** | **无套利信号** | - |

**ETHFI的Z-score = 0.78** 属于 **正常波动范围**（\|Z\| ≤ 1）

---

## 实际意义

### 1. 对于ETHFI/USDC:USDC

**当前状态**：
- 当前价差 = 历史均值 + 0.78 × 标准差
- 价差略高于历史平均水平，但**偏离程度不大**

**交易含义**：
- ❌ **套利信号不足**：偏离程度（0.78σ）未达到显著水平（≥2σ）
- ⚠️ **价差偏高**：ETHFI相对BTC的价格关系略高于历史平均
- ✅ **正常波动**：0.78σ的偏离在正常市场波动范围内

### 2. 为什么需要阈值 ≥ 2.0？

系统设定Z-score阈值 ≥ 2.0的原因：

**统计学原理**：
- 在正态分布中，**95%的数据落在±2σ范围内**
- **|Z| > 2** 意味着当前价差处于**极端5%的尾部区域**
- 基于均值回归理论，极端偏离后更可能回归

**实际交易考虑**：
- **|Z| < 2**：偏离不够显著，可能是正常波动，而非真正的套利机会
- **|Z| ≥ 2**：偏离足够显著，触发均值回归的概率更高
- **交易成本**：只有当偏离足够大时，才能覆盖交易手续费和滑点

### 3. ETHFI当前情况

**Z-score = 0.78，距阈值2.0差1.22**：
- 当前偏离程度 = 0.78σ
- 需要偏离 = 2.0σ
- **还需要额外的1.22σ偏离**才能触发告警

**这意味着**：
- 如果历史标准差为0.01（例如），当前价差需要再偏离 **1.22 × 0.01 = 0.0122** 才会触发
- 或者，如果ETHFI相对BTC的价格关系进一步扩大，Z-score可能会上升

---

## 可视化理解

```
价差分布（正态分布假设）：

          -3σ    -2σ    -1σ    0    +1σ    +2σ    +3σ
           |      |      |     |     |      |      |
价差 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           ▼      ▼             ▲     ▲      ▲      ▲
        做多信号 做多信号      均值   当前   做空信号 做空信号
        (Z<-2)  (Z=-1.5)       (0)  (0.78) (Z>2)  (Z=2.5)
                              

ETHFI当前位置：+0.78σ（略高于均值，但未达到做空信号阈值+2σ）
```

---

## 与阈值2.0的对比

### 如果Z-score = 2.0，意味着：
- 当前价差 = 历史均值 + 2.0 × 标准差
- 处于历史分布的**97.7百分位数**
- 只有**2.3%的历史数据**的价差高于当前值
- 这是**显著的偏离**，触发做空信号（如果价差过高）或做多信号（如果价差过低）

### ETHFI当前Z-score = 0.78：
- 处于历史分布的**78.2百分位数**
- **21.8%的历史数据**的价差高于当前值
- **偏离不够显著**，属于正常波动范围

---

## 总结

**Z-score = 0.78的含义**：

1. **数值上**：当前价差比历史均值高0.78个标准差
2. **统计学上**：处于历史分布的第78百分位数，属于正常波动范围
3. **交易上**：偏离程度不足，未达到套利信号阈值（需要≥2.0）
4. **实际意义**：ETHFI相对BTC的价格关系略高于历史平均，但这种偏离仍在正常范围内，不足以触发交易信号

**为什么没有触发告警**：
- 系统设定阈值 = 2.0（确保只有显著偏离才触发）
- ETHFI的Z-score = 0.78 < 2.0，偏离不够显著
- 需要等待价差进一步扩大（Z-score ≥ 2.0）才会触发告警



